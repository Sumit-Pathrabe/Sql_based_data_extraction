# Sql_based_data_extraction
SQL-based data extraction project in Google Colab. I created a custom dataset and used SQL queries (SELECT, WHERE, GROUP BY, JOIN) to answer business questions, then converted results to pandas DataFrames for analysis and reporting.
